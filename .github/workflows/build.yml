name: build

on:
  push:
    branches: [ 'master' ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: molehunt
          path: |
            data
            pack.mcmeta
